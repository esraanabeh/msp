post:
    tags:
      - Quotes
    summary: Check Client Quote Link
    description: 'Check Client Quote Link with this API'
    operationId: check-quote-link
    consumes:
    - multipart/form-data
    parameters:
    - name: request body
      in: body
      schema:
        type: object
        required:
          - code
        properties:
          code:
            type: string
    responses:
      '200':
        description: successful operation
      '400':
        description: bad request
      '422':
        description: Validation exception

get:
  tags:
    - FAQ
  summary: Get All FAQ
  description: 'Get All FAQ '
  operationId: FAQ
  security:
  - bearerAuth: []
  consumes:
    - application/json
  produces:
    - application/json
  parameters:
    - name: id
      in: path
      type: integer
      required: true
      schema:
        type: integer
        required:
        properties:
  responses:
    '200':
      description: successful operation
    '400':
      description: Bad request

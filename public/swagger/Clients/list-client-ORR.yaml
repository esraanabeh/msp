get:
  tags:
    - Clients
  summary: Get All Client ORR Services
  description: 'Get All Client ORR Services'
  operationId: Client ORR Services
  security:
  - bearerAuth: []
  consumes:
    - application/json
  produces:
    - application/json
  parameters:
    - name: clientId
      in: path
      type: integer
      required: true
  responses:
    '200':
      description: successful operation
    '400':
      description: Bad request

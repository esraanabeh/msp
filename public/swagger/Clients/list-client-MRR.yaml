get:
  tags:
    - Clients
  summary: Get All Client MRR Services
  description: 'Get All Client MRR Services'
  operationId: Client MRR Services
  security:
  - bearerAuth: []
  consumes:
    - application/json
  produces:
    - application/json
  parameters:
    - name: clientId
      in: path
      type: integer
      required: true
  responses:
    '200':
      description: successful operation
    '400':
      description: Bad request
